{% extends 'base.html.twig' %}

{% block title %}Jeux video d occaz{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        Salut tout le monde
        <ul>
            {% for user in users %}
                <li>{{user.firstName}} {{user.lastName}} </li>
            {% endfor %}
        </ul>
        <ul>
            {% for jeux in jeuxvideo %}
                <li>{{jeux.name}} - {{jeux.price}} € </li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endblock %}
