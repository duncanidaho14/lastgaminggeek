{% extends "base.html.twig" %}

{% block title %}
    Modifier son Jeux video
{% endblock %}

{% block body %}
    <div class="row">
        <div class="container">
            <h1>Editer </h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                    retourner au près de l'article
                    <a href="{{path('article_show', {'slug': jeuxvideo.slug})}}">{{jeuxvideo.name}}</a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}