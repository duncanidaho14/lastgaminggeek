{% block _jeuxvideo_categories_widget %}
    <p>Ici, vous pouvez ajouter vos propres catégories !</p>

    {{ form_widget(form) }}

    <input type="hidden" id="widgets-counter" value="0">

    <div class="form-group">
        <button type="button" id="add-categories" class="btn btn-primary">
            <i class="fas fa-plus"></i>
            Ajouter une catégorie
        </button>
    </div>
{% endblock %}

{% block _jeuxvideo_categories_entry_row %}
    {{ form_widget(form) }}
{% endblock %}

{% block _jeuxvideo_categories_entry_widget %}
    <div class="form-group" id="block_{{id}}">
    {{ form_errors(form) }}
        <div class="row">
            <div class="col-10">
                <div class="row">
                    <div class="col">
                        {{ form_widget(form.name) }}
                        {{ form_errors(form.name) }}
                    </div>
                    <div class="col">
                        {{ form_widget(form.imageFile) }}
                        {{ form_errors(form.imageFile) }}
                    </div>
                </div>
            </div>
            <div class="col-2">
                <button type="button" data-action="delete" data-target="#block_{{id}}" class="btn btn-danger">X</button>
            </div>
        </div>
    </div>
{% endblock %}


{# block de commentaires #}

{% block _jeuxvideo_comments_widget %}
    <p>Ici, vous pouvez ajouter vos propres catégories !</p>

    {{ form_widget(form) }}

    <input type="hidden" id="widgets-counter" value="0">

    <div class="form-group">
        <button type="button" id="add-comments" class="btn btn-primary">
            <i class="fas fa-plus"></i>
            Ajouter un commentaire
        </button>
    </div>
{% endblock %}

{% block _jeuxvideo_comments_entry_row %}
    {{ form_widget(form) }}
{% endblock %}

{% block _jeuxvideo_comments_entry_widget %}
    <div class="form-group" id="block_{{id}}">
    {{ form_errors(form) }}
        <div class="row">
            <div class="col-10">
                <div class="row">
                    <div class="col">
                        {{ form_widget(form.title) }}
                        {{ form_errors(form.title) }}
                    </div>
                    <div class="col">
                        {{ form_widget(form.comment) }}
                        {{ form_errors(form.comment) }}
                    </div>
                </div>
            </div>
            <div class="col-2">
                <button type="button" data-action="delete" data-target="#block_{{id}}" class="btn btn-danger">X</button>
            </div>
        </div>
    </div>
{% endblock %}
