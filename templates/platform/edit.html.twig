{% extends 'base.html.twig' %}

{% block title %}Edit Platform{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Edit Platform</h1>

        {{ include('platform/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('platform_index') }}">back to list</a>

        {{ include('platform/_delete_form.html.twig') }}
    </div>
</div>
    
{% endblock %}

{% block javascripts %}
    <script src="{{asset('build/app.js')}}"></script>

    <script>
    window.addEventListener('DOMContentLoaded', function() {
        AOS.init();
    });

    </script>
{% endblock %}